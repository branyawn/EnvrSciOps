# EnvrSciOps
EnvrSciOps is an AI‑augmented environmental data platform that ingests public environmental APIs on a schedule, stores them in a relational database, applies SQL transformations + validations, and publishes an analytics layer for dashboards—with an AI toolkit that accelerates documentation, SQL review, and pipeline debugging.
